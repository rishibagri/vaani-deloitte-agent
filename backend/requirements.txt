fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
python-dotenv==1.0.1
google-genai>=0.8.0
numpy==1.23.5
librosa==0.10.1
httpx==0.27.0
pyyaml>=6.0

# Persistent face-recognition memory (PostgreSQL)
# Install separately if needed: pip install cmake dlib face_recognition
psycopg2-binary>=2.9.0

# Supabase semantic memory layer (optional — leave SUPABASE_URL blank to skip)
supabase>=2.0.0
