# GPU utility packages — PyTorch and torchvision are installed separately
# by scripts\setup.bat with the correct CUDA index URL (auto-detected).
# Do NOT pip install this file standalone — use scripts\setup.bat instead.
opencv-python==4.9.0.80
diffusers==0.30.2
accelerate==0.28.0
omegaconf>=2.3.0
